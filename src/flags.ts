// Feature flags — flip these to enable/disable features for testing or rollout.

export const FLAGS = {
  /** Show Google AdSense banners (production only). */
  ADS_ENABLED: false,

  /** Require wins / purchases to unlock premium card themes. */
  THEME_GATES_ENABLED: true,
}
